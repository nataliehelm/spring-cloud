server:
port: ${PORT:8888}

spring:
application:
name: config-server
cloud:
config:
server:
git:
uri: https://github.com/vaninagodoydh/spring-cloud-fintech-gateway
default-label: main   //brach en github
force-pull: true
search-paths: service-configuration   // directorio dentro de github
